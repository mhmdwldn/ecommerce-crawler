# Alertmanager config — routes alerts to webhook
route:
  receiver: "default"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 3h

receivers:
  - name: "default"
    webhook_configs: []
